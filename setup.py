from setuptools import setup, find_packages

setup(name='Hash',
      version=1.5,
      author='oimq',
      url='https://github.com/oimq/Hash',
      author_email='taep0q@gmail.com',
      description='JSON format handler with json module',
      packages=find_packages(),
      install_requires=[],
      zip_safe=False
      )